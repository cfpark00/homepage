# Supabase
NEXT_PUBLIC_SUPABASE_URL=your_supabase_project_url
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key

# Site URL (for production)
# Vercel automatically sets NEXT_PUBLIC_VERCEL_URL, but you can override with:
# NEXT_PUBLIC_SITE_URL=https://your-portal-domain.vercel.app/

# Set up Google OAuth in Supabase Dashboard:
# 1. Go to Authentication > Providers in your Supabase project
# 2. Enable Google provider
# 3. Add your Google OAuth credentials
# 4. Set redirect URL to: http://localhost:3021/auth/callback (for local development)
# 5. For production, use: https://yourdomain.com/auth/callback